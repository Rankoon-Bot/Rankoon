import { Component, inject } from '@angular/core'; import { Observable } from 'rxjs'; import { AnalyticsPageComponent } from './analytics-page.component'; import { AnalyticsPageBase } from './analytics-page.base'; import { AnalyticsRange, GuildAnalyticsVoice } from './guild-analytics.models'; import { GuildAnalyticsService } from './guild-analytics.service';
@Component({ selector: 'app-analytics-voice', standalone: true, imports: [AnalyticsPageComponent], template: `<app-analytics-page page="voice" [range]="range()" [data]="data()" [loading]="loading()" [error]="error()" (rangeChange)="changeRange($event)" (retry)="load()" />` })
export class AnalyticsVoiceComponent extends AnalyticsPageBase<GuildAnalyticsVoice> { private readonly api = inject(GuildAnalyticsService); constructor() { super(); this.initialize(); } protected request(id: string, range: AnalyticsRange): Observable<GuildAnalyticsVoice> { return this.api.voice(id, range); } }
